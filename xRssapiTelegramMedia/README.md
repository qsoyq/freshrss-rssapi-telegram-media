# Extension files

Copy this entire directory to FreshRSS's `extensions/` directory, then enable **RSSAPI Telegram Media** under **Administration > Extensions**. See the repository [README](../README.md) for requirements and configuration.
